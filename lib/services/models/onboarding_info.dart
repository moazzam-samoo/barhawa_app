//this class is for saving and calling the info of onboarding, it will create a data
class OnboardingInfo{
  final imageAsset;
  final title;
  final description;

  OnboardingInfo(this.imageAsset, this.title, this.description);
}
